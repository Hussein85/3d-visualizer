# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /tags                       controllers.Application.tags
POST     /language                   controllers.Application.language
GET     /model/add                   controllers.Model.addForm
POST    /model/add                   controllers.Model.upload
GET     /model/alltags              controllers.Model.allTags

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Enable webjar based resources to be returned
GET     /webjars/*file              controllers.WebJarAssets.at(file)

GET /javascriptRoutes  controllers.Application.javascriptRoutes